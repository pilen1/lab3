package src;

import java.awt.*;

public abstract class lastbil extends Vehicle {
    public lastbil(int nrDoors, double enginePower, Color color, String modelName, int x, int y) {
        super(nrDoors, enginePower, color, modelName, x, y);
    }
}
